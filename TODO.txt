- check capital letters for Router, App in require dependencies
- consider bower usage
  use npm to install bower and gulp
  use gulp to run bower and install all other dependencies
- add validation with backbone validator plugin
- organize require imports:
  use syntax sugar for require
- review npm+bower dependencies - which one are dev deps?
- final code cleanup:
  dont forget spacing
